<?php

use Illuminate\Database\Seeder;
use Illuminate\Database\Eloquent\Model;
use App\Sp_type;

class SptypeTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
		
		// Sp_type::insert([
		//     'type'=>'ROADS',
		// ]);

		// Sp_type::insert([
		//     'type'=>'WATER SYSTEM',
		// ]);

		// Sp_type::insert([
		//     'type'=>'BHS',
		// ]);

		// Sp_type::insert([
		//     'type'=>'PATHWAY',
		// ]);

		// Sp_type::insert([
		//     'type'=>'DRAINAGE',
		// ]);

		// Sp_type::insert([
		//     'type'=>'EVACUATION CENTER',
		// ]);

		// Sp_type::insert([
		//     'type'=>'FOOTBRIDGE',
		// ]);

		// Sp_type::insert([
		//     'type'=>'SEA WALL',
		// ]);

		// Sp_type::insert([
		//     'type'=>'MULTI PURPOSE BUILDING',
		// ]);

		// Sp_type::insert([
		//     'type'=>'TRIBAL CENTER',
		// ]);

		// Sp_type::insert([
		//     'type'=>'EPSL',
		// ]);

		// Sp_type::insert([
		//     'type'=>'SCHOOL BUILDING',
		// ]);

		// Sp_type::insert([
		//     'type'=>'CULVERTS',
		// ]);

		// Sp_type::insert([
		//     'type'=>'SPSL',
		// ]);

		// Sp_type::insert([
		//     'type'=>'DCC',
		// ]);

		// Sp_type::insert([
		//     'type'=>'FLOOD CONTROL',
		// ]);

		// Sp_type::insert([
		//     'type'=>'LATRINE',
		// ]);

		// Sp_type::insert([
		//     'type'=>'SCHOOL BUILDING',
		// ]);

		// Sp_type::insert([
		//     'type'=>'WHARF',
		// ]);

		// Sp_type::insert([
		//     'type'=>'RIVER DIKE',
		// ]);

		// Sp_type::insert([
		//     'type'=>'RICE MILL',
		// ]);

		// Sp_type::insert([
		//     'type'=>'SLOPE PROTECTION',
		// ]);

		// Sp_type::insert([
		//     'type'=>'STAIRWAY',
		// ]);

		// Sp_type::insert([
		//     'type'=>'BRIDGES',
		// ]);

		// Sp_type::insert([
		//     'type'=>'RIVERBANK PROTECTION',
		// ]);

		// Sp_type::insert([
		//     'type'=>'RWH',
		// ]);

		// Sp_type::insert([
		//     'type'=>'SOLAR DRYER',
		// ]);

		// Sp_type::insert([
		//     'type'=>'LEARNING CENTER',
		// ]);

		// Sp_type::insert([
		//     'type'=>'OTHERS',
		// ]);

    }
}
